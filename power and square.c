#include<stdio.h>
#include<math.h>
int main()
{
	float x,y;
	printf("enter the values of a and y :");
	scanf("%f%f",&x,&y);
	printf("x raised to power y is : %f \n",pow(x,y));
	printf("square root of x is : %f \n",sqrt(x));
	printf("square root of y is : %f \n",sqrt(y));
	return 0;
}
